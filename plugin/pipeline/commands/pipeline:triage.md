---
description: Set a pre-pipeline stage label (ready or backlog) on issue N
argument-hint: '<N> --stage <ready|backlog>'
---

Invoke: `node ${CLAUDE_PLUGIN_ROOT}/skills/pipeline/scripts/pipeline.mjs triage $ARGUMENTS`

Run synchronously (completes in seconds). No background process or Monitor needed.
