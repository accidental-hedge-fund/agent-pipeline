---
description: Run deterministic preflight checks and print a pass/fail summary (opt-in --harness-smoke for role-aware runtime smoke)
argument-hint: '[--harness-smoke]'
---

Invoke: `node ${CLAUDE_PLUGIN_ROOT}/skills/pipeline/scripts/pipeline.mjs doctor $ARGUMENTS`

Run synchronously (completes in seconds). No background process or Monitor needed.
