{
  "schema_version": 1,
  "recipe_id": "restart-and-reentry",
  "summary": "Stop the supervisor during an active item, resume from a fresh process, and prove that same-head re-entry is a no-op.",
  "steps": [
    {
      "id": "stop-process",
      "action": "Stop the supervisor after it records an active Pipeline run identity."
    },
    {
      "id": "resume-process",
      "action": "Start a fresh supervisor process and resume the same loop run."
    },
    {
      "id": "repeat-same-head",
      "action": "Repeat the completed same-head step and record that no duplicate mutation occurs."
    }
  ],
  "required_evidence": [
    { "kind": "action-result", "minimum": 3 },
    { "kind": "pipeline-events", "minimum": 2 },
    { "kind": "github-observation", "minimum": 1 }
  ],
  "scenario_outputs": [
    { "id": "resume-mid-flight" }
  ],
  "composition_outputs": [
    { "id": "process-restart-hydration" },
    { "id": "same-head-noop-reentry" }
  ]
}
