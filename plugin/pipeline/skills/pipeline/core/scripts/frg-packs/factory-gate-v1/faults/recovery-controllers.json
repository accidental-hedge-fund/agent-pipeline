{
  "schema_version": 1,
  "recipe_id": "recovery-controllers",
  "summary": "Exercise the production recovery controller once through pipeline single and once through pipeline loop.",
  "steps": [
    {
      "id": "single-controller",
      "action": "Run one injected recoverable outcome through pipeline single and record bounded recovery success."
    },
    {
      "id": "loop-controller",
      "action": "Run one injected recoverable outcome through pipeline loop and record bounded recovery success."
    }
  ],
  "required_evidence": [
    { "kind": "action-result", "minimum": 2 },
    { "kind": "pipeline-events", "minimum": 2 }
  ],
  "scenario_outputs": [],
  "composition_outputs": [
    { "id": "recovery-controller-one-item" },
    { "id": "recovery-controller-multi-item" }
  ]
}
