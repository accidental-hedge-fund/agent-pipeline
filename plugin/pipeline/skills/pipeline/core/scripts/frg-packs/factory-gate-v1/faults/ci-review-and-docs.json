{
  "schema_version": 1,
  "recipe_id": "ci-review-and-docs",
  "summary": "Exercise pending-to-red-to-green CI, a blocking review fix, re-review, and local documentation parity.",
  "steps": [
    {
      "id": "inject-ci-failure",
      "action": "Make one controlled required check move from pending to red."
    },
    {
      "id": "record-fix-rereview",
      "action": "Record one blocking finding, its fix commit, and a passing re-review on that head."
    },
    {
      "id": "verify-docs-parity",
      "action": "Run the local documentation freshness gate before ready-to-deploy."
    }
  ],
  "required_evidence": [
    { "kind": "action-result", "minimum": 2 },
    { "kind": "pipeline-events", "minimum": 2 },
    { "kind": "check-observation", "minimum": 2 },
    { "kind": "github-observation", "minimum": 1 }
  ],
  "scenario_outputs": [
    { "id": "local-docs-parity" }
  ],
  "composition_outputs": [
    { "id": "fix-rereview-cycle" },
    { "id": "ci-pending-red-recovery" }
  ]
}
