{
  "name": "pipeline",
  "version": "1.12.0",
  "description": "Self-contained Codex pipeline skill — advances issues through a label-driven state machine to ready-to-deploy",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "js-yaml": "^4.1.0",
    "yaml": "^2.9.0",
    "zod": "^4.0.0"
  }
}
