## 1. Catalog and shared contract

- [x] 1.1 Add `train` and `ship` to `OPERATION_SURFACE` as explicit operator-authorized verbs (same summaries as today's `command-docs.ts`) and verify they appear when the catalog is enumerated.
- [x] 1.2 Add `core/scripts/host-skill.ts` with the single deep rendering seam `renderHostSkill(options?)`; its optional `operationSurface` and `manifests` inputs support deterministic tests while production defaults use `OPERATION_SURFACE` and `loadOuterHostManifestsPreferHosts()`. Export the issue-locked `SKILL_HOST_IDS = ["claude", "codex", "grok", "opencode"] as const` as the sole generated-host membership set, with no notify values or behavior. Select those manifests in tuple order, fail closed on any missing or duplicate selected ID, and exclude non-selected OMP. Derive notify values only from each selected manifest's `material_progress_notify.mapping`. The contract retains the default numeric `pipeline <N>` drive, names `run_id`, distinguishes `pipeline logs <advance-run-id> --events --follow` from `pipeline loop logs <loop-run-id> --events --follow`, requires reattach, stop-on-terminal/supervisor-exit, terminal reason and final summary, and carries the forbid-list (`merge`, `merge-queue --apply`, `train --merge`, `ship`).
- [x] 1.3 Export `renderHostSkill` so #971 can consume the same bytes without copying a host essay. Verify there is no Hermes/OpenClaw install path in this change (`git diff` does not add `examples/supervisor` install logic).
- [x] 1.4 Add manifest/render parity coverage using injected fixtures: compare each rendered notify row with the Claude, Codex, Grok, or OpenCode manifest mapping; prove a fixture mapping change changes the rendered row; and fail on mapping divergence, missing selected IDs, or duplicate selected IDs. Add a set-parity guard binding `SKILL_HOST_IDS` to the rendered rows and build write/check targets. Add a source-of-truth bite guard over `host-skill.ts` and shared orchestration that fails if code introduces an independent notify-value map keyed by host id or branches on host id to dispatch notify behavior, while explicitly allowing the membership-only `SKILL_HOST_IDS` tuple. Add an exact conformance fixture whose notify declaration has neither a usable surface nor a portable fallback and prove it fails for that missing-capability reason. Keep the existing host-manifest/core-builtin byte-parity guard green.

## 2. Generator

- [x] 2.1 Make `scripts/build.mjs` the sole writer for whole host SKILL bytes, deriving `hosts/<id>/SKILL.md` write and check targets from `SKILL_HOST_IDS`. Verify the derived set is exactly `hosts/claude/SKILL.md`, `hosts/codex/SKILL.md`, `hosts/grok/SKILL.md`, and `hosts/opencode/SKILL.md`; fail a drift test if ID, row, write-target, or check-target membership differs; and prove `build.mjs --check` fails after a one-byte edit to any target.
- [x] 2.2 Remove all host-SKILL inputs, reads, marker checks, OMP requirements, rewrites, and output artifacts from `core/scripts/docs-generate.ts` and `scripts/generate-docs.mjs`. Retarget `core/test/docs-generate.test.ts` so docs generation covers only its remaining docs/CHANGELOG outputs.
- [x] 2.3 Change plugin SKILL generation in `scripts/build.mjs` to call `renderHostSkill` directly. Verify it neither reads `hosts/claude/SKILL.md` nor calls the retired marked-region renderer, and it does not restore the Setup/Required essay.
- [x] 2.4 Keep `scripts/build.mjs --check` responsible for the four host SKILLs, transitional plugin SKILL, and marketplace catalog. Verify write and check mode use the same exact SKILL/catalog target list without broadening the hook to other plugin-shell assets.
- [x] 2.5 Assert build and install write no `/pipeline:*` markdown command files and no Codex `pipeline-<verb>.yaml` agents. Verify the existing #1048 command-pack test still fails if those paths are written.

## 3. Host artifacts

- [x] 3.1 Generate and commit `hosts/claude/SKILL.md`, `hosts/codex/SKILL.md`, `hosts/grok/SKILL.md`, and `hosts/opencode/SKILL.md`. Verify byte identity and the exact absolute GitHub links to `docs/packaging.md` and `docs/cli.md` from an installed-tree context.
- [x] 3.2 Delete `hosts/omp/SKILL.md`; set `install.overlayFiles` to `[]` in both `hosts/omp/outer-host.manifest.json` and `core/scripts/outer-hosts/builtins/omp.json`. Verify those manifests remain byte-identical and OMP tree install still stages core, launcher, native command, and managed marker without a SKILL.
- [x] 3.3 Preserve Grok's mirrored outer-host manifests and installer behavior as `symlink-claude`, `overlayDir: "hosts/claude"`, and `overlayFiles: []`. Verify `hosts/grok/SKILL.md` is only a byte-identical repository conformance output, `install --host grok` still materializes only the safe Claude-managed symlink, and no lifecycle test moves to tree mode.
- [x] 3.4 Update `README.md`, `docs/packaging.md`, and Grok manifest/post-install prose to document both facts without contradiction: the generated `hosts/grok/SKILL.md` exists, while the installed Grok path remains the Claude-managed symlink.
- [x] 3.5 Run `node scripts/build.mjs` so all four host SKILLs, the plugin SKILL, and catalog match one generation. Verify `node scripts/build.mjs --check` is green.

## 4. Tests and living-spec retargets

- [x] 4.1 Add exact build/freshness tests for all four generated host SKILL paths, including a deliberate one-byte stale-file failure, byte parity, default numeric drive, both exact advance/loop follow commands, no retired essays, `train`/`ship` table parity, absolute doc links, direct plugin renderer use, and the compact autonomous-through-ready-to-deploy/operator-owned-merge policy (including merge-queue dry-run and ship phrase mapping). Add a hostile fixture/source assertion that fails if the shared renderer contains a stage inventory, stage-handler dispatch, or other host-specific stage logic even when all four generated outputs would remain byte-identical. Add semantic contract fixtures beyond command-presence checks: after the `loop_item_advance_linked` event's `pipeline_run_id` is retained as the advance id, the loop follow stays active while the linked advance follow is added; a later linkage or terminal advance replaces/stops the prior advance follow; and premature supervisor exit tears down every run-scoped follow but produces only a non-terminal failure/recovery report, never a completion summary.
- [x] 4.2 Update `.githooks/pre-commit`, `scripts/pre-commit-hook.test.mjs`, and `core/test/pre-commit-hook.test.ts` so renderer/operation-surface/build edits and each exact `hosts/{claude,codex,grok,opencode}/outer-host.manifest.json` input trigger regeneration, and so the unstaged/untracked-input guards cover those exact manifests. Stage all four host SKILL outputs plus the existing plugin/catalog outputs by exact path. Preserve the guards against unrelated `hosts/` or `plugin/` dirt and prove an unrelated host path still neither triggers nor stages output.
- [x] 4.3 Update eval generated-packaging accounting in `core/scripts/evals/preflight.ts` and its tests to include all four exact host SKILL outputs and require them for renderer, operation-surface, build, or manifest inputs. Do not replace exact paths with a broad `hosts/` or `plugin/` allowance.
- [x] 4.4 Remove host SKILLs from post-tag `generate-docs.mjs` ownership (`core/scripts/release-docs-refresh.ts`) and update its tests; build owns those files after this change. Update release clean-precondition and rollback tests so all four build-owned host SKILL paths are detected and restored or rejected safely.
- [x] 4.5 Retarget stage-inventory, native-`/goal`, loop-facade, factory-policy, Monitor-grep, release-plan, human-lessons, harness-adapter, artifact-ignore, installed-engine-freshness, namespaced-command, README-clarity, and outer-host orchestration tests off the deleted essays and onto CLI help, README, durable docs, manifests, or `renderHostSkill` as specified. Retarget the existing dual-follow/re-attach/terminal-stop guards to assert loop-follow continuity after linkage, prior-advance replacement, terminal teardown, and premature-exit-is-not-completion. Add an offline drift guard that compares durable documentation with every entry in `ADVANCE_MATERIAL_KINDS`, `LOOP_MATERIAL_KINDS`, `LOOP_OPTIONAL_MATERIAL_KINDS`, `LOOP_PROGRESS_DEFINITIVE_STATUSES`, and the shared suppression rules, and prove a deletion from each class fails. Verify every retargeted test still fails on its named drift.
- [x] 4.6 Consolidate the deleted essay's durable operator content in `docs/cli.md`, `docs/concepts.md`, `docs/packaging.md`, or README as each modified capability requires: five-adapter login/configuration; the installed-engine update command; release-plan row shape; native-`/goal` bootstrap; advance reattach/status/logs/final-summary steps; linked-loop and material-event follow/filter detail; plus the stage inventory, loop selectors, lessons workflow, and artifact paths. Verify every removed SKILL assertion has an outcome-equivalent durable-doc assertion rather than being deleted for a green suite.

## 5. Validation

- [x] 5.1 Run `openspec validate generate-short-host-skill --strict` and the repository-required `openspec validate --all`; fix every error introduced by this change. Record the existing repo-wide strict baseline separately rather than treating unrelated legacy strict failures as this issue's gate.
- [x] 5.2 Run `npm run ci` from the repo root and verify it is green (core tests, `build.mjs --check`, install smoke, OpenSpec, docs freshness, scripts).
- [x] 5.3 Check `proposal.md` acceptance criteria against the tree (four short SKILLs, OMP SKILL gone, no command pack, shared contract, `train`/`ship` on the surface, `npm run ci` green) and record any miss before calling the change done.
