# ack-gate-needs-human-only-for-operator-text

The human-ack gate parks at `needs-human` only for real operator scope-change text. Trusted review headings with a terminal artifact, and closed operational notes, never stop unattended ship.
