## 1. Biting regressions (inject I/O; prove fail first)

- [x] 1.1 Add an injected park-release / automatic-remove test: clean managed worktree, remote head absent, local-only reports squash-merge unreachability or `null`, **`VerifiedMergeProof` present** (minted by `createVerifiedMergeProof` / `proveMergeResultInBase` for issue N / PR P / configured base / proven OID). Assert the worktree is **removed** and the result/reason does **not** contain `commit verification failed (git/network/auth error)` or "check connectivity". Verify this test **fails** against current code (today it retains with git/network/auth). No live network, git, or subprocess.
- [x] 1.2 Add an injected case with the same post-squash-merge unreachability and **no** bound proof. Assert the worktree is **retained** and the reason names commits not reachable from the base (existing squash-merge / `--force` wording). Assert the reason does **not** contain `commit verification failed (git/network/auth error)`. Verify this test **fails** if the current `null` path still emits git/network/auth. Same I/O seam as 1.1.
- [x] 1.3 Add injected cases: (a) bound proof + **dirty** tree → retain, reason names dirty, not git/network/auth, remove seam not called; (b) bound proof + clean tree + remove seam throws filesystem/cleanup error → retain **only that worktree** with that error, not git/network/auth, and no label/integration rollback call. Verify both fail if implementation force-discards dirty trees or maps cleanup failure to git/network/auth.
- [x] 1.4 Add four independent identity-mismatch cases: wrong issue, wrong PR, wrong base, wrong OID. Each MUST NOT release this worktree. Verify the test **fails** if unmatched proof authorizes remove.
- [x] 1.5 Add injected classifier cases with a `gitCmd` seam (same pattern as `CreateWorktreeDeps.gitCmd`): (a) `ls-remote` code 0 + empty SHA, then `git log` non-empty or exit 1 → `"unverifiable"`, not `null`; (b) `ls-remote` code 0 + empty SHA, then log/command error (exit ≥ 128) → `"unverifiable"`, not `null`; (c) `ls-remote` non-zero (transport) → `null`. Verify (a)/(b) **fail** against current `checkLocalOnlyCommits` returning `null`. Do not collapse exit-status 1 (non-ancestry) with `ls-remote` transport failure.
- [x] 1.6 Add ordering cases: matching proof MUST NOT bypass `underManagedRoot === false`, dirty, or `localOnly === true`. Remove is not called.
- [x] 1.7 Add `createVerifiedMergeProof` runtime-validation tests: reject non-positive issue/PR, empty base, non-40-char OID. A raw `{ issue, pr, base, oid }` object or a log/label string is not accepted as proof by the gate.
- [x] 1.8 Add an injected park-release case: matching bound proof + clean tree + unverifiable local-only, but `hasRemoteBranchTip` / `resolveOpenPrHeadForBranch` throw. Assert the worktree is **released** and those probes are not called.
- [x] 1.9 Add an injected park-release / automatic-remove case: matching bound proof + remote deleted + unverifiable local-only + current HEAD differs from the merge-time worktree HEAD. Assert the worktree is **retained** (later local work) and remove is not called.
- [x] 1.10 Add an injected `gitProveMergeResultDeps` case: `git fetch origin <base>` exits non-zero while a stale local `origin/<base>` still contains the merge OID (ancestry would succeed). Assert `proveMergeResultInBase` returns null, ancestry is not tested, and park-release **retains** the worktree. Verify this test **fails** if fetch failure is ignored.

## 2. Shared classifier and ladder

- [x] 2.1 Change local-only verification so an observed missing remote head (successful empty `ls-remote` or equivalent) plus not-reachable-from-base is classified as squash-merge unreachability, not `null` / git/network/auth. Keep `null` only when the transport cannot tell whether the remote head exists. Verify tasks 1.2 and 1.5 pass. Do not treat a dirty tree as clean.
- [x] 2.2 Add `VerifiedMergeProof`, `createVerifiedMergeProof`, and `proveMergeResultInBase`. Extend `evaluateRemoveSafety` with `boundProofMatches?: boolean` (do not put identity matching inside the pure table). When `boundProofMatches` and the tree is clean, `"unverifiable"` / `null` SHALL NOT retain. Do **not** set `force: true` to skip unverifiable. Verify task 1.1 passes and a dirty+proof case still retains (task 1.3a).
- [x] 2.3 Thread `verifiedMergeProof` through `removeManagedWorktreeSafely` and `releaseWorktreeForParkedIssue` (single `evaluateRemoveSafety` per decision). Match proof identity at the wrapper (`boundProofMatches(proof, { issue, pr, base: cfg.base_branch })`). Matching proof is recoverability condition 2 in park-release (skip remote-tip / open-PR retain). Verify `core/test/worktree-remove-safety-registry.test.ts` still classifies both sites as ladder-backed. Verify task 1.3b (cleanup error) reports the remove-seam message.

## 3. Callers: deploy_ready, pipeline park, train --merge

- [x] 3.1 `deploy_ready.finalize`: after ready-to-deploy labels/summary are recorded, if the linked PR is merged, mint `VerifiedMergeProof` via `proveMergeResultInBase` (observe OID + fetch base + `isAncestor`) and pass it into `removeManagedWorktreeSafely`. Verify the injected path releases a clean tree and does **not** log `worktree retained after ready-to-deploy (commit verification failed (git/network/auth error); check connectivity and retry)`. Verify a caller-seam test that `finalize` passes the same proof object into the shared wrapper (not a deploy_ready-only skip). Cleanup failure MUST NOT call `setBlocked` or drop `pipeline:ready-to-deploy`.
- [x] 3.2 `maybeReleaseWorktreeOnPark` / `/pipeline` / `pipeline single`: pass `verifiedMergeProof` through to `releaseWorktreeForParkedIssue`. Verify an injected advance-finalize/park case matching 1.1 releases. Assert the park caller invokes `releaseWorktreeForParkedIssue` with the proof rather than only testing the gate in isolation. Cleanup failure and dirty retain MUST NOT change `pipeline:ready-to-deploy`.
- [x] 3.3 After `train --merge` records `train_merge_proven` / `train_merge_integrated`, mint proof from the successful in-base verifier in that same call (do not re-read events) and invoke `releaseWorktreeForParkedIssue` with it. Do not add a train-only `git worktree remove`. Verify an injected train merge case matching 1.1 releases; dirty retain does not clear `integrated` and does not emit connectivity guidance. Verify a test fails if `train.ts` contains a `git worktree remove` / `removeWorktree` path that bypasses the shared gate. Update the remove-safety registry if train gains a release call — it must be ladder-backed.

## 4. Docs, mirror, CI

- [x] 4.1 Update operator-facing park-release / worktree docs so they distinguish: bound-proof release, squash-merge unreachability without proof (retain + existing `--force` wording), and true git/network/auth. Verify the docs mention `/pipeline` and `train --merge` share the gate, and that `/pipeline:cleanup` is not the required fix.
- [x] 4.2 After any `core/` edit, run `node scripts/build.mjs` and include regenerated `plugin/` in the same change. Verify `node scripts/build.mjs --check` is clean.
- [x] 4.3 Run `openspec validate park-release-bound-merge-proof` and `npm run ci` from the repo root. Verify both are green. Do not claim suite pass without that evidence. Do not merge inside advance/loop. Do not add `auto_merge`. Do not weaken review.
