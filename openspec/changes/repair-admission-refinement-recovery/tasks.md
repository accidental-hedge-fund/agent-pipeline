## 1. Lock Reproductions at Owning Boundaries

- [ ] 1.1 Preserve and review the existing admission candidate on this issue branch, then retain the renderer regressions proving the integration-base body expands beyond the supported ceiling and lacks unique-oversize refusal; verify the tests exercise real `makeNode`/`embedDecisionsInBody` behavior and use no external I/O.
- [ ] 1.2 Add a planning regression through `makeOpenspecPlanningHooks.revalidateArtifact` and `buildImplPlan` in which substantive accepted revision stdout leaves the proposal/tasks unchanged; verify the integration-base behavior rereads the original proposal and omits the accepted refinement from implementation input.
- [ ] 1.3 Add a `realDispatchItem` regression whose child event stream ends with `tester_rebind_pr_head_unobservable` and whose subsequent injected forge refresh throws; verify the integration-base response loses the precise diagnostic before the repair.
- [ ] 1.4 Add a retained-state RecoverySupervisor regression with coarse workflow-engine evidence, matching linked terminal events, two spent `unlink_engine_scratch` attempts, and zero legacy class budget; verify the integration-base outer eligibility gate suppresses the unspent applicable `rebind_tester_evidence_after_pr` strategy.

## 2. Complete Decisions Admission Repair

- [ ] 2.1 Finalize the additive digest-keyed evidence catalog and reference expansion in `grill-decisions.ts`; verify repeated node and authority-request evidence is stored once only when total body size improves and parsing restores the complete semantic artifact.
- [ ] 2.2 Preserve inline `decisions.v1` compatibility and all authority/provenance/input-digest/content-hash checks; verify old inline fixtures and compact fixtures both round-trip and tampered, missing, colliding, or unresolved references fail closed.
- [ ] 2.3 Keep the full-body 65,536-character check before every publication caller; verify the eleven-node reproduced body fits, genuinely unique oversize content throws before injected issue mutation, no evidence/node is truncated, and unrelated issue sections survive unchanged.

## 3. Bind Accepted OpenSpec Refinement to Implementation

- [ ] 3.1 Capture the authoritative OpenSpec artifact state around revision using existing change-id, worktree, file-read, and validation seams; verify a substantive accepted refinement must materially affect the identified change rather than merely satisfy `plan-revision.ack@1`.
- [ ] 3.2 Revalidate and reread the coherent proposal, tasks, and spec deltas after revision, then construct implementation input only from that validated authoritative state; verify the accepted refined content reaches the implementation prompt exactly and no prior-proposal/stdout fallback is used.
- [ ] 3.3 Return an explicit engine-owned OpenSpec failure for unchanged, unapplied, missing, incoherent, or structurally invalid refined artifacts; verify implementation does not start and the prior accepted plan is not posted or delivered as revised.
- [ ] 3.4 Run the paired freeform planning tests and verify the non-OpenSpec stdout-based refinement path and existing authority/review gates remain unchanged.

## 4. Preserve and Recover Precise Diagnostics

- [ ] 4.1 Reorder `realDispatchItem` observation so confirmed child events are parsed before fallible forge refreshes and retained independently; verify the injected forge-outage regression returns the precise child diagnostic plus explicit observation uncertainty without a gate pass or fabricated Tester binding.
- [ ] 4.2 At blocked recovery preflight, reuse `SupervisorDeps.readAdvanceEvents` to refine coarse persisted evidence from terminal linked events only when retained run ID, item, candidate, and canonical run-store location match; verify matching #1558-shaped evidence selects the Tester-rebind diagnostic without editing events or episode history.
- [ ] 4.3 Add negative linked-event tests for missing, malformed, arbitrary-path, run-mismatched, item-mismatched, and non-terminal streams; verify each remains fail-closed on persisted evidence and cannot confer Tester success or a replacement episode.

## 5. Align Recovery Admission with Per-Strategy Authority

- [ ] 5.1 Reuse or extract one pure Recovery Episode eligibility predicate covering lifecycle/Cooling, diagnostic applicability, cursor, attempts per strategy, and per-strategy bounds; verify both outer blocked-item eligibility and execution-time selection use it while class remaining budget stays compatibility-only.
- [ ] 5.2 Replay the retained coarse-to-precise same-episode case and verify `rebind_tester_evidence_after_pr` is claimable with its unspent bound despite zero class projection, while both prior scratch attempts and all budget accounting remain unchanged.
- [ ] 5.3 Add finite-boundary regressions proving exhausted strategies stay ineligible, inapplicable recipes remain skips, no applicable unspent strategy enters owned Cooling/wait, repeated evidence cannot tight-loop, and candidate/evidence episode identity is preserved.
- [ ] 5.4 Add a scheduler regression proving an independent sibling remains eligible to advance or recover while another item is Cooling or waiting.

## 6. Verify and Hand Off

- [ ] 6.1 Run the focused renderer, planning, dispatch, durable-classification, and recovery test files with Node's injected-I/O test harness; verify the original bounded reproductions pass and no test performs real network, git, or subprocess operations.
- [ ] 6.2 Run `node scripts/build.mjs` after all `core/` edits and verify `node scripts/build.mjs --check` reports every generated host SKILL fresh without creating or committing `plugin/`.
- [ ] 6.3 Run `npm run ci` from the repository root and verify core tests, host build freshness, install smoke, OpenSpec validation, conditional docs freshness, and scripts checks all pass.
- [ ] 6.4 Complete ordinary independent Pipeline reviews and GitHub CI on the exact candidate and verify they are green; stop at ready-to-deploy because merge, release, tag, publication, installation, and deployment require separate authorization.
