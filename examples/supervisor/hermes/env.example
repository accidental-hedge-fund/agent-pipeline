# Copy to a mode-0600 host file (e.g. ~/.config/pipeline-supervisor/env).
# Do not commit real secrets or private channel IDs. This file is a template only.

# Target repo checkout for pipeline cwd
REPO_DIR=/home/YOU/dev/agent-pipeline-factory-control

# Prefer the codex skill launcher (or whichever install you maintain)
# Multi-word is OK if your unit sources this and wraps carefully; otherwise use
# a single PATH entry via examples/supervisor/shell/pipeline-launcher.sh
PIPELINE=/usr/bin/node
# Example alternate: PIPELINE=/home/YOU/.local/bin/pipeline-launcher

# If using "node …/pipeline.mjs", set the full command via a wrapper script on PATH.
# Recommended: install pipeline-launcher.sh as ~/.local/bin/pipeline

# Clone of agent-pipeline that contains examples/supervisor/shell/
AGENT_PIPELINE_ROOT=/home/YOU/dev/agent-pipeline

# Set to 1 only for a private, allowlisted channel that may merge / release finish
ALLOW_MERGE=0

# Durable ship / notify state (host-local)
PIPELINE_SUPERVISOR_STATE=/home/YOU/.local/state/pipeline-supervisor

# Ship playbook notify defaults
SHIP_NOTIFY=1
SHIP_NOTIFY_HEARTBEAT_S=0
# SHIP_NOTIFY_BIN=$AGENT_PIPELINE_ROOT/examples/supervisor/shell/ship-notify.sh
# SHIP_STAGE_WATCH_BIN=$AGENT_PIPELINE_ROOT/examples/supervisor/shell/ship-stage-watch.sh
# ENGINE_PROMOTE_HOST=codex

# Optional Buzz messenger (ship-notify no-ops if unset)
# BUZZ_BIN=/path/to/buzz
# BUZZ_RELAY_URL=https://example.invalid
# BUZZ_CHANNEL=
# BUZZ_CREDENTIALS_FILE=/path/to/buzz-credentials.json
