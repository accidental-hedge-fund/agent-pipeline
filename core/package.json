{
  "name": "pipeline",
  "version": "1.40.1",
  "description": "Self-contained Codex pipeline skill — advances issues through a label-driven state machine to ready-to-deploy",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --import ./test/helpers/isolated-observability.ts --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "js-yaml": "^4.3.0",
    "yaml": "^2.9.0",
    "zod": "^4.0.0"
  }
}
